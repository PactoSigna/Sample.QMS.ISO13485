name: QMS Validation

on:
  pull_request:
    paths:
      - 'docs/**'
  push:
    branches:
      - main
    paths:
      - 'docs/**'

permissions:
  contents: read
  pull-requests: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run document validation
        id: validate
        run: |
          set +e
          OUTPUT=$(npx @pactosigna/schemas@latest validate docs/ 2>&1)
          EXIT_CODE=$?
          echo "$OUTPUT" > validation-result.json

          # Strip any npm stderr lines before the JSON
          JSON_OUTPUT=$(echo "$OUTPUT" | grep -v '^npm ' | tail -n +1)
          echo "$JSON_OUTPUT" > validation-result.json
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"

          # Parse summary for display
          TOTAL=$(echo "$JSON_OUTPUT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['summary']['total'])")
          PASSED=$(echo "$JSON_OUTPUT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['summary']['passed'])")
          FAILED=$(echo "$JSON_OUTPUT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['summary']['failed'])")
          WARNINGS=$(echo "$JSON_OUTPUT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['summary']['warnings'])")

          echo "### Document Validation Results" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Metric | Count |" >> "$GITHUB_STEP_SUMMARY"
          echo "|--------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Total | $TOTAL |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Passed | $PASSED |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Failed | $FAILED |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Warnings | $WARNINGS |" >> "$GITHUB_STEP_SUMMARY"

          if [ "$EXIT_CODE" -ne 0 ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "#### Errors" >> "$GITHUB_STEP_SUMMARY"
            echo '```json' >> "$GITHUB_STEP_SUMMARY"
            echo "$JSON_OUTPUT" | python3 -c "
          import sys,json
          d=json.load(sys.stdin)
          for r in d['results']:
            if r['errors']:
              print(f\"{r['path']} ({r['documentId']}):\")
              for e in r['errors']:
                print(f\"  - [{e['code']}] {e['message']}\")
          " >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Comment on PR
        if: github.event_name == 'pull_request' && steps.validate.outputs.exit_code != '0'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const result = JSON.parse(fs.readFileSync('validation-result.json', 'utf8'));
            const { summary, results } = result;
            const errors = results.filter(r => r.errors.length > 0);

            let body = `### Document Validation Failed\n\n`;
            body += `**${summary.failed}** of ${summary.total} documents have errors.\n\n`;

            for (const doc of errors) {
              body += `#### \`${doc.path}\` (${doc.documentId})\n`;
              for (const e of doc.errors) {
                body += `- \`${e.code}\`: ${e.message}\n`;
              }
              body += '\n';
            }

            body += `\n---\n*Validated with @pactosigna/schemas*`;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body,
            });

      - name: Fail if validation errors
        if: steps.validate.outputs.exit_code != '0'
        run: exit 1

      - name: Clean up
        if: always()
        run: rm -f validation-result.json
