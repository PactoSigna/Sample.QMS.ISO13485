# Auto-generated — run `pnpm generate:schemas` to regenerate
schema_version: "1.0"
document_type: management_review
label: "Management Review"
scope: qms
folder: unknown
id_prefixes: []

frontmatter:
  id:                 { required: true, type: string }
  title:              { required: true, type: string }
  status:             { required: false, type: enum, values: ["draft", "scheduled", "completed"] }
  type:               { required: false, inferred: true, notes: "inferred from folder path; omit unless overriding" }
  version:            { required: false, type: string }
  review_date:        { required: true, type: string }
  review_period:      { required: true, type: unknown }
  attendees:          { required: true, type: object[] }
  data_snapshot_date: { required: false, type: string }
  next_review_date:   { required: false, type: string }
  author:             { required: false, type: email, notes: "email address of primary author" }
  reviewers:          { required: false, type: string[], notes: "department names e.g. Engineering" }
  approvers:          { required: false, type: string[], notes: "department names e.g. Quality" }

sections:
  required:
    - Review Inputs
    - Review Outputs
    - Action Items
    - Decisions
