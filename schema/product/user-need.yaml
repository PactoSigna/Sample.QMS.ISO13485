# Auto-generated — run `pnpm generate:schemas` to regenerate
schema_version: "1.0"
document_type: user_need
label: "User Need"
scope: device
folder: docs/product/user-needs
id_prefixes: [UN-]

frontmatter:
  id:                { required: true, type: string }
  title:             { required: true, type: string }
  status:            { required: true, type: enum, values: ["draft", "in_review", "approved", "effective", "archived", "example"] }
  type:              { required: false, inferred: true, notes: "inferred from folder path; omit unless overriding" }
  stakeholder:       { required: true, type: string }
  priority:          { required: false, type: enum, values: ["must_have", "should_have", "nice_to_have"] }
  source:            { required: false, type: string }
  derives:           { required: false, type: string[] }
  use_specification: { required: false, type: string }
  author:            { required: false, type: email, notes: "email address of primary author" }
  reviewers:         { required: false, type: string[], notes: "department names e.g. Engineering" }
  approvers:         { required: false, type: string[], notes: "department names e.g. Quality" }

sections:
  required:
    - User Story
    - Validation Criteria
