# Auto-generated — run `pnpm generate:schemas` to regenerate
schema_version: "1.0"
document_type: software_risk
label: "Software Risk"
scope: device
folder: docs/software/risks
id_prefixes: [RISK-SW-]

frontmatter:
  id:                      { required: true, type: string }
  title:                   { required: true, type: string }
  status:                  { required: true, type: enum, values: ["draft", "in_review", "approved", "effective", "archived"] }
  type:                    { required: false, inferred: true, notes: "inferred from folder path; omit unless overriding" }
  analyzes:                { required: true, type: string }
  hazardous_situation:     { required: true, type: string }
  harm:                    { required: true, type: string }
  inherent_severity:       { required: true, type: integer, min: 1, max: 5 }
  inherent_probability:    { required: false, type: integer, min: 1, max: 5 }
  inherent_exploitability: { required: false, type: integer, min: 1, max: 5 }
  mitigations:             { required: false, type: object[] }
  residual_severity:       { required: false, type: integer, min: 1, max: 5 }
  residual_probability:    { required: false, type: integer, min: 1, max: 5 }
  residual_exploitability: { required: false, type: integer, min: 1, max: 5 }
  author:                  { required: false, type: email, notes: "email address of primary author" }
  reviewers:               { required: false, type: string[], notes: "department names e.g. Engineering" }
  approvers:               { required: false, type: string[], notes: "department names e.g. Quality" }

sections:
  required: []
